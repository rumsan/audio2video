# API key (required — change this before deploying)
API_KEY=changeme

# Server
HOST=0.0.0.0
PORT=8000

# Public base URL used to build progress/download URLs returned to callers
BASE_URL=http://localhost:8000

# Local directories (relative to working directory, created automatically)
CACHE_DIR=cache
OUTPUT_DIR=output
